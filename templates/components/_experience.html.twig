<section id="experience" class="max-w-3xl mx-auto lg:mx-0">
    {# <h2 class="text-3xl lg:text-4xl font-bold mb-6 lg:mb-8">{{ 'sections.experiences'|trans }}</h2> #}
    <div class="relative space-y-8">
        <!-- Vertical Line -->
        <div class="absolute left-2 top-2 bottom-2 w-0.5 bg-base-300"></div>

        {% for experience in experiences %}
            <!-- Experience Item -->
            <div class="relative pl-8">
                <!-- Timeline Dot -->
                <div class="absolute left-[9px] top-6 w-3 h-3 -translate-x-1/2 rounded-full bg-primary border-2 border-base-100"></div>
                
                <!-- Content -->
                <div class="space-y-4">
                    <!-- Header -->
                    <div class="flex flex-col sm:flex-row items-start gap-4">
                        <div class="shrink-0 w-16 sm:w-12 h-16 sm:h-12 rounded-xl bg-base-300/50 p-2 flex items-center justify-center">
                            <img src="{{ asset('images/' ~ experience.logo) }}" alt="{{ experience.company }} Logo" class="w-full h-full object-contain">
                        </div>
                        <div>
                            <div class="flex flex-wrap items-center gap-x-2 gap-y-1">
                                <h3 class="text-xl font-bold" data-trans="{{ experience.title_key }}">{{ experience.title_key|trans }}</h3>
                                <span class="text-base-content/70" data-trans="experiences.at_experience">{{ 'experiences.at_experience'|trans }}</span>
                                <span class="font-semibold">{{ experience.company }}</span>
                            </div>
                            <div class="flex flex-wrap gap-4 text-sm text-base-content/70 mt-1">
                                <div class="flex items-center gap-1">
                                    <i class="fa-solid fa-location-dot"></i>
                                    <span data-trans="{{ experience.location_key }}">{{ experience.location_key|trans }}</span>
                                </div>
                                <div class="flex items-center gap-1">
                                    <i class="fa-regular fa-calendar"></i>
                                    <span data-trans="{{ experience.period_key }}">{{ experience.period_key|trans }}</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Description -->
                    <div class="space-y-2 text-base-content/70">
                        {% for desc_key in experience.description_keys %}
                            <p data-trans="{{ desc_key }}">{{ desc_key|trans }}</p>
                        {% endfor %}
                    </div>

                    <!-- Technologies -->
                    <div class="flex flex-wrap gap-1.5">
                        {% for tech in experience.technologies %}
                            <div class="badge badge-md px-2 py-2 bg-{{ tech.color_bg }} text-{{ tech.color_text }} hover:bg-{{ tech.color_hover }} transition-colors border-none">
                                {% if tech.icon is defined %}
                                    <i class="{{ tech.icon }} mr-1"></i>
                                {% else %}
                                    <img src="{{ asset('images/' ~ tech.image) }}" alt="{{ tech.label_key|trans }}" class="w-3 h-3 mr-1">
                                {% endif %}
                                <span data-trans="{{ tech.label_key }}">{{ tech.label_key|trans }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</section>
